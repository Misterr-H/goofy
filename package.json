{
  "name": "goofy-monorepo",
  "private": true,
  "workspaces": [
    "packages/client",
    "packages/backend"
  ],
  "dependencies": {
    "dotenv": "^8.2.0",
    "node-fetch": "^3.3.2"
  },
  "scripts": {
    "backend:dev": "npm run dev --workspace=packages/backend",
    "backend:build": "npm run build --workspace=packages/backend",
    "backend:start": "npm run start --workspace=packages/backend"
  }
}
